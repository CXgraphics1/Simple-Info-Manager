{
  "name": "info-manager",
  "version": "1.0.0",
  "description": "A simple program to prompt a user's input in console and store the response in JSON.",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git"
  },
  "author": "CXgraphics",
  "license": "MIT",
  "bugs": {

  },
  "dependencies": {
    "ascii-table": "^0.0.9",
    "enquirer": "^2.3.6",
    "jsonfile-updater": "^3.1.0"
  }
}
